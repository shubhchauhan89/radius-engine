# ─── Radius SEO Proxy — Apache Rewrite Rules ─────────────────────────────────
# Paste these lines inside your existing .htaccess file (usually in the web root).
# They MUST appear BEFORE any CMS rewrite rules (e.g., WordPress, Laravel).
# ──────────────────────────────────────────────────────────────────────────────

RewriteEngine On

# Route all /locations/* requests to the Radius edge proxy
RewriteRule ^locations/(.*)$ radius.php [L,QSA]

# for new htaccess file

<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /

    # --- S2 Logic: Radius Engine Routing ---
    RewriteRule ^locations/?$ radius.php [L,QSA]
    RewriteRule ^locations/(.+)$ radius.php [L,QSA]
    # ---------------------------------------
</IfModule>
